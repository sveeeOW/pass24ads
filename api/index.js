const fs = require('fs');
const path = require('path');

const newDetailCode = "function openBannerDetail(id) {\n      const bannerId = String(id || '').trim();\n      if (!bannerId) return;\n      const { date1, date2 } = getSelectedRange();\n      const names = getMergedNames();\n      const meta = getMergedMeta();\n      const name = names[bannerId] || inferredName(bannerId);\n      const metaRow = meta[bannerId] || {};\n      const days = getDateRangeDays(date1, date2);\n      const dayMap = new Map();\n      (lastPayload.bannerDaily || []).forEach((row) => {\n        if (String(row.id) !== bannerId) return;\n        if (row.date < date1 || row.date > date2) return;\n        if (!dayMap.has(row.date)) dayMap.set(row.date, { clicks: 0, users: 0 });\n        const item = dayMap.get(row.date);\n        item.clicks += Number(row.clicks || 0);\n        item.users += Number(row.users || 0);\n      });\n      const impressionsMap = new Map();\n      (lastPayload.impressionsDaily || []).forEach((row) => {\n        if (!row.date || row.date < date1 || row.date > date2) return;\n        impressionsMap.set(row.date, Number(row.impressions || row.value || 0));\n      });\n      const rows = days.map((date) => {\n        const item = dayMap.get(date) || { clicks: 0, users: 0 };\n        return { date, impressions: impressionsMap.get(date) || 0, clicks: item.clicks, users: item.users };\n      });\n      const clicksTotal = rows.reduce((sum, row) => sum + Number(row.clicks || 0), 0);\n      const usersTotal = rows.reduce((sum, row) => sum + Number(row.users || 0), 0);\n      const impressionsTotal = rows.reduce((sum, row) => sum + Number(row.impressions || 0), 0);\n      const placement = (metaRow.start || metaRow.end) ? ` · размещение: ${formatDateRu(metaRow.start)} — ${formatDateRu(metaRow.end)}` : '';\n\n      currentDetailReport = {\n        id: bannerId,\n        name,\n        date1,\n        date2,\n        placementStart: metaRow.start || '',\n        placementEnd: metaRow.end || '',\n        url: metaRow.url || '',\n        rows,\n        totals: { impressions: impressionsTotal, clicks: clicksTotal, users: usersTotal, days: rows.length },\n      };\n\n      els.detailTitle.textContent = `ID ${bannerId} — ${name}`;\n      els.detailSubtitle.textContent = `${formatDateRu(date1)} — ${formatDateRu(date2)}${placement}`;\n      els.detailClicksTotal.textContent = humanNumber(clicksTotal);\n      els.detailUsersTotal.textContent = humanNumber(usersTotal);\n      els.detailImpressionsTotal.textContent = humanNumber(impressionsTotal);\n      els.detailDaysTotal.textContent = humanNumber(rows.length);\n      els.detailTableBody.innerHTML = rows.map((row) => `\n        <tr>\n          <td class=\"mono\">${formatDateRu(row.date)}</td>\n          <td class=\"mono\">${humanNumber(row.impressions)}</td>\n          <td class=\"mono\">${humanNumber(row.clicks)}</td>\n          <td class=\"mono\">${humanNumber(row.users)}</td>\n        </tr>\n      `).join('');\n      els.detailOverlay.classList.add('open');\n    }\n    function xmlEscape(v) {\n      return String(v ?? '')\n        .replace(/&/g, '&amp;')\n        .replace(/</g, '&lt;')\n        .replace(/>/g, '&gt;')\n        .replace(/\"/g, '&quot;')\n        .replace(/'/g, '&apos;');\n    }\n    function excelCol(n) {\n      let s = '';\n      while (n > 0) {\n        const m = (n - 1) % 26;\n        s = String.fromCharCode(65 + m) + s;\n        n = Math.floor((n - 1) / 26);\n      }\n      return s;\n    }\n    function sheetXml(rows, merges = []) {\n      const rowXml = rows.map((row, rIdx) => {\n        const cells = row.map((cell, cIdx) => {\n          const ref = `${excelCol(cIdx + 1)}${rIdx + 1}`;\n          const style = cell.s ? ` s=\"${cell.s}\"` : '';\n          if (cell.t === 'n') return `<c r=\"${ref}\"${style}><v>${Number(cell.v || 0)}</v></c>`;\n          if (cell.t === 'date') return `<c r=\"${ref}\"${style} t=\"inlineStr\"><is><t>${xmlEscape(formatDateRu(cell.v))}</t></is></c>`;\n          return `<c r=\"${ref}\"${style} t=\"inlineStr\"><is><t>${xmlEscape(cell.v)}</t></is></c>`;\n        }).join('');\n        return `<row r=\"${rIdx + 1}\">${cells}</row>`;\n      }).join('');\n      const mergeXml = merges.length ? `<mergeCells count=\"${merges.length}\">${merges.map(ref => `<mergeCell ref=\"${ref}\"/>`).join('')}</mergeCells>` : '';\n      return `<?xml version=\"1.0\" encoding=\"UTF-8\" standalone=\"yes\"?>\n<worksheet xmlns=\"http://schemas.openxmlformats.org/spreadsheetml/2006/main\" xmlns:r=\"http://schemas.openxmlformats.org/officeDocument/2006/relationships\">\n  <sheetViews><sheetView workbookViewId=\"0\"/></sheetViews>\n  <sheetFormatPr defaultRowHeight=\"18\"/>\n  <cols><col min=\"1\" max=\"1\" width=\"18\"/><col min=\"2\" max=\"2\" width=\"18\"/><col min=\"3\" max=\"3\" width=\"16\"/><col min=\"4\" max=\"4\" width=\"18\"/><col min=\"5\" max=\"5\" width=\"36\"/></cols>\n  <sheetData>${rowXml}</sheetData>\n  ${mergeXml}\n</worksheet>`;\n    }\n    function workbookXml() {\n      return `<?xml version=\"1.0\" encoding=\"UTF-8\" standalone=\"yes\"?>\n<workbook xmlns=\"http://schemas.openxmlformats.org/spreadsheetml/2006/main\" xmlns:r=\"http://schemas.openxmlformats.org/officeDocument/2006/relationships\"><sheets><sheet name=\"Результат по рекламе\" sheetId=\"1\" r:id=\"rId1\"/></sheets></workbook>`;\n    }\n    function workbookRelsXml() {\n      return `<?xml version=\"1.0\" encoding=\"UTF-8\" standalone=\"yes\"?>\n<Relationships xmlns=\"http://schemas.openxmlformats.org/package/2006/relationships\"><Relationship Id=\"rId1\" Type=\"http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet\" Target=\"worksheets/sheet1.xml\"/><Relationship Id=\"rId2\" Type=\"http://schemas.openxmlformats.org/officeDocument/2006/relationships/styles\" Target=\"styles.xml\"/></Relationships>`;\n    }\n    function rootRelsXml() {\n      return `<?xml version=\"1.0\" encoding=\"UTF-8\" standalone=\"yes\"?>\n<Relationships xmlns=\"http://schemas.openxmlformats.org/package/2006/relationships\"><Relationship Id=\"rId1\" Type=\"http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument\" Target=\"xl/workbook.xml\"/></Relationships>`;\n    }\n    function contentTypesXml() {\n      return `<?xml version=\"1.0\" encoding=\"UTF-8\" standalone=\"yes\"?>\n<Types xmlns=\"http://schemas.openxmlformats.org/package/2006/content-types\"><Default Extension=\"rels\" ContentType=\"application/vnd.openxmlformats-package.relationships+xml\"/><Default Extension=\"xml\" ContentType=\"application/xml\"/><Override PartName=\"/xl/workbook.xml\" ContentType=\"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml\"/><Override PartName=\"/xl/worksheets/sheet1.xml\" ContentType=\"application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml\"/><Override PartName=\"/xl/styles.xml\" ContentType=\"application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml\"/></Types>`;\n    }\n    function stylesXml() {\n      return `<?xml version=\"1.0\" encoding=\"UTF-8\" standalone=\"yes\"?>\n<styleSheet xmlns=\"http://schemas.openxmlformats.org/spreadsheetml/2006/main\">\n  <fonts count=\"3\"><font><sz val=\"11\"/><name val=\"Arial\"/></font><font><b/><sz val=\"14\"/><color rgb=\"FFFFFFFF\"/><name val=\"Arial\"/></font><font><b/><sz val=\"11\"/><name val=\"Arial\"/></font></fonts>\n  <fills count=\"4\"><fill><patternFill patternType=\"none\"/></fill><fill><patternFill patternType=\"gray125\"/></fill><fill><patternFill patternType=\"solid\"><fgColor rgb=\"FF0F766E\"/><bgColor indexed=\"64\"/></patternFill></fill><fill><patternFill patternType=\"solid\"><fgColor rgb=\"FFE0F2FE\"/><bgColor indexed=\"64\"/></patternFill></fill></fills>\n  <borders count=\"2\"><border><left/><right/><top/><bottom/><diagonal/></border><border><left style=\"thin\"><color rgb=\"FFD1D5DB\"/></left><right style=\"thin\"><color rgb=\"FFD1D5DB\"/></right><top style=\"thin\"><color rgb=\"FFD1D5DB\"/></top><bottom style=\"thin\"><color rgb=\"FFD1D5DB\"/></bottom><diagonal/></border></borders>\n  <cellStyleXfs count=\"1\"><xf numFmtId=\"0\" fontId=\"0\" fillId=\"0\" borderId=\"0\"/></cellStyleXfs>\n  <cellXfs count=\"5\"><xf numFmtId=\"0\" fontId=\"0\" fillId=\"0\" borderId=\"0\" xfId=\"0\"/><xf numFmtId=\"0\" fontId=\"1\" fillId=\"2\" borderId=\"1\" xfId=\"0\" applyFont=\"1\" applyFill=\"1\" applyBorder=\"1\"/><xf numFmtId=\"0\" fontId=\"2\" fillId=\"3\" borderId=\"1\" xfId=\"0\" applyFont=\"1\" applyFill=\"1\" applyBorder=\"1\"/><xf numFmtId=\"0\" fontId=\"0\" fillId=\"0\" borderId=\"1\" xfId=\"0\" applyBorder=\"1\"/><xf numFmtId=\"0\" fontId=\"2\" fillId=\"3\" borderId=\"1\" xfId=\"0\" applyFont=\"1\" applyFill=\"1\" applyBorder=\"1\"/></cellXfs>\n</styleSheet>`;\n    }\n    function crc32(bytes) {\n      let table = crc32.table;\n      if (!table) {\n        table = crc32.table = new Uint32Array(256);\n        for (let i = 0; i < 256; i++) {\n          let c = i;\n          for (let k = 0; k < 8; k++) c = c & 1 ? 0xEDB88320 ^ (c >>> 1) : c >>> 1;\n          table[i] = c >>> 0;\n        }\n      }\n      let c = 0xffffffff;\n      for (let i = 0; i < bytes.length; i++) c = table[(c ^ bytes[i]) & 0xff] ^ (c >>> 8);\n      return (c ^ 0xffffffff) >>> 0;\n    }\n    function u16(n){ return [n & 255, (n >>> 8) & 255]; }\n    function u32(n){ return [n & 255, (n >>> 8) & 255, (n >>> 16) & 255, (n >>> 24) & 255]; }\n    function makeZip(files) {\n      const enc = new TextEncoder();\n      const chunks = [];\n      const central = [];\n      let offset = 0;\n      files.forEach((file) => {\n        const name = enc.encode(file.name);\n        const data = enc.encode(file.content);\n        const crc = crc32(data);\n        const local = new Uint8Array([0x50,0x4b,0x03,0x04, ...u16(20), ...u16(0x0800), ...u16(0), ...u16(0), ...u16(0), ...u32(crc), ...u32(data.length), ...u32(data.length), ...u16(name.length), ...u16(0)]);\n        chunks.push(local, name, data);\n        central.push({ name, crc, size: data.length, offset });\n        offset += local.length + name.length + data.length;\n      });\n      const centralStart = offset;\n      central.forEach((file) => {\n        const dir = new Uint8Array([0x50,0x4b,0x01,0x02, ...u16(20), ...u16(20), ...u16(0x0800), ...u16(0), ...u16(0), ...u16(0), ...u32(file.crc), ...u32(file.size), ...u32(file.size), ...u16(file.name.length), ...u16(0), ...u16(0), ...u16(0), ...u16(0), ...u32(0), ...u32(file.offset)]);\n        chunks.push(dir, file.name);\n        offset += dir.length + file.name.length;\n      });\n      const centralSize = offset - centralStart;\n      const eocd = new Uint8Array([0x50,0x4b,0x05,0x06, ...u16(0), ...u16(0), ...u16(central.length), ...u16(central.length), ...u32(centralSize), ...u32(centralStart), ...u16(0)]);\n      chunks.push(eocd);\n      return new Blob(chunks, { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });\n    }\n    function exportCurrentDetailReport() {\n      if (!currentDetailReport) return;\n      const r = currentDetailReport;\n      const reportTitle = `Результат по рекламе ${r.name}`;\n      const placementPeriod = (r.placementStart || r.placementEnd) ? `${formatDateRu(r.placementStart)} — ${formatDateRu(r.placementEnd)}` : '—';\n      const rows = [\n        [{v:reportTitle, s:1},{v:'', s:1},{v:'', s:1},{v:'', s:1}],\n        [{v:'', s:0}],\n        [{v:'ID баннера', s:2},{v:r.id, s:3}],\n        [{v:'Период отчёта', s:2},{v:`${formatDateRu(r.date1)} — ${formatDateRu(r.date2)}`, s:3}],\n        [{v:'Период размещения', s:2},{v:placementPeriod, s:3}],\n        [{v:'Ссылка', s:2},{v:r.url || '—', s:3}],\n        [{v:'', s:0}],\n        [{v:'ИТОГО', s:2},{v:'', s:2},{v:'', s:2},{v:'', s:2}],\n        [{v:'Показы', s:3},{v:r.totals.impressions, t:'n', s:4}],\n        [{v:'Клики', s:3},{v:r.totals.clicks, t:'n', s:4}],\n        [{v:'Пользователи', s:3},{v:r.totals.users, t:'n', s:4}],\n        [{v:'', s:0}],\n        [{v:'Дата', s:2},{v:'Показы', s:2},{v:'Клики', s:2},{v:'Пользователи', s:2}],\n        ...r.rows.map(row => ([{v:row.date, t:'date', s:3},{v:row.impressions, t:'n', s:3},{v:row.clicks, t:'n', s:3},{v:row.users, t:'n', s:3}])),\n        [{v:'ИТОГО', s:4},{v:r.totals.impressions, t:'n', s:4},{v:r.totals.clicks, t:'n', s:4},{v:r.totals.users, t:'n', s:4}],\n      ];\n      const files = [\n        {name:'[Content_Types].xml', content:contentTypesXml()},\n        {name:'_rels/.rels', content:rootRelsXml()},\n        {name:'xl/workbook.xml', content:workbookXml()},\n        {name:'xl/_rels/workbook.xml.rels', content:workbookRelsXml()},\n        {name:'xl/styles.xml', content:stylesXml()},\n        {name:'xl/worksheets/sheet1.xml', content:sheetXml(rows, ['A1:D1','A8:D8'])},\n      ];\n      const blob = makeZip(files);\n      const safeName = String(r.name || `banner-${r.id}`).replace(/[\\\\/:*?\"<>|]+/g, '-').replace(/\\s+/g, '_').slice(0, 60);\n      const a = document.createElement('a');\n      a.href = URL.createObjectURL(blob);\n      a.download = `PASS24_${r.id}_${safeName}_${r.date1}_${r.date2}.xlsx`;\n      document.body.appendChild(a);\n      a.click();\n      setTimeout(() => { URL.revokeObjectURL(a.href); a.remove(); }, 1000);\n    }\n";

function patchDashboard(html) {
  if (html.includes('detailImpressionsTotal') && html.includes('exportCurrentDetailReport')) return html;

  html = html.replace(
    `<div class="detail-stat">
            <div class="label">Пользователи за период</div>
            <div class="value mono" id="detailUsersTotal">0</div>
          </div>
          <div class="detail-stat">`,
    `<div class="detail-stat">
            <div class="label">Пользователи за период</div>
            <div class="value mono" id="detailUsersTotal">0</div>
          </div>
          <div class="detail-stat">
            <div class="label">Показы за период</div>
            <div class="value mono" id="detailImpressionsTotal">0</div>
          </div>
          <div class="detail-stat">`
  );
  html = html.replace(
    `Ниже показаны все дни выбранного диапазона. Если в конкретный день у баннера не было событий в AppMetrica, день остаётся в таблице с нулями.`,
    `Ниже показаны все дни выбранного диапазона. Показы берутся из общей дневной статистики AppMetrica и одинаковы для всех баннеров за соответствующий день. Если в конкретный день у баннера не было событий в AppMetrica, клики и пользователи остаются нулевыми.`
  );
  html = html.replace(
    `<div class="detail-table-wrap">`,
    `<div style="margin: 0 0 14px;"><button class="btn primary" id="exportDetailXlsxBtn" type="button">Скачать Excel</button></div>
        <div class="detail-table-wrap">`
  );
  html = html.replace(
    `<th style="width: 180px;">Дата</th>
                <th style="width: 140px;">Клики</th>
                <th style="width: 160px;">Пользователи</th>`,
    `<th style="width: 180px;">Дата</th>
                <th style="width: 140px;">Показы</th>
                <th style="width: 140px;">Клики</th>
                <th style="width: 160px;">Пользователи</th>`
  );
  html = html.replace(
    `detailUsersTotal: document.getElementById('detailUsersTotal'),
      detailDaysTotal: document.getElementById('detailDaysTotal'),
      detailTableBody: document.getElementById('detailTableBody'),`,
    `detailUsersTotal: document.getElementById('detailUsersTotal'),
      detailImpressionsTotal: document.getElementById('detailImpressionsTotal'),
      detailDaysTotal: document.getElementById('detailDaysTotal'),
      detailTableBody: document.getElementById('detailTableBody'),
      exportDetailXlsxBtn: document.getElementById('exportDetailXlsxBtn'),`
  );
  html = html.replace(
    `let editorCurrentId = null;`,
    `let editorCurrentId = null;
    let currentDetailReport = null;`
  );
  html = html.replace(new RegExp('function openBannerDetail\\(id\\) \\{[\\s\\S]*?\\n    function closeBannerDetail\\(\\) \\{'), `${newDetailCode}    function closeBannerDetail() {`);
  html = html.replace(
    `els.closeDetailBtn.addEventListener('click', closeBannerDetail);
      els.detailOverlay.addEventListener('click', (e) => {`,
    `els.closeDetailBtn.addEventListener('click', closeBannerDetail);
      els.exportDetailXlsxBtn.addEventListener('click', exportCurrentDetailReport);
      els.detailOverlay.addEventListener('click', (e) => {`
  );
  return html;
}

module.exports = function handler(req, res) {
  try {
    const indexPath = path.join(process.cwd(), 'index.html');
    const html = fs.readFileSync(indexPath, 'utf8');
    res.setHeader('Content-Type', 'text/html; charset=utf-8');
    res.setHeader('Cache-Control', 'no-store, max-age=0');
    res.status(200).send(patchDashboard(html));
  } catch (error) {
    res.status(500).send('Failed to render dashboard: ' + (error && error.message ? error.message : String(error)));
  }
};
